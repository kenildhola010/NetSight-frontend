
  # Netsight

  This is a code bundle for Netsight. The original project is available at https://www.figma.com/design/18wKZp4ryx75Nv2YTaihdl/Netsight.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  